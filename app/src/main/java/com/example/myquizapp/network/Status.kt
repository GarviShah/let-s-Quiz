package com.example.myquizapp.network

/** Status is an enum class that represents the state of a resource. It can be SUCCESS, ERROR, or LOADING. */
enum class Status {
    SUCCESS,
    ERROR,
    LOADING
}